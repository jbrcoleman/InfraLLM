# Terraform Backend Configuration
# Generated by InfraLLM on {{ generated_at }}

terraform {
  backend "s3" {
    bucket         = "{{ organization }}-terraform-state"
    key            = "{{ environment }}/{{ resource_type }}/{{ resource_name }}/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "{{ organization }}-terraform-locks"
  }
}
